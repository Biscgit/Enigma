unit-integration-test:
  stage: test
  image: ${CI_REGISTRY_IMAGE}/python-tester-linter:cl-19
  before_script:
    - cd backend
  script:
    - python3 -m pytest --cov --junitxml=report.xml --cov-report term --cov-report xml:coverage.xml
  coverage: '/TOTAL.*\s+(\d+%)$/'
  artifacts:
    when: always
    paths:
      - backend/report.xml
      - backend/coverage.xml
    reports:
      junit: backend/report.xml
      coverage_report:
        coverage_format: cobertura
        path: backend/coverage.xml
  rules:
    - exists:
      - backend/server/test/*
    - changes:
      - "backend/**/*"
  needs:
    - job: build-python-tester-linter
      optional: true

e2e-test:
  stage: e2e-test
  image: ${CI_REGISTRY_IMAGE}/flutter-builder:latest
  before_script:
    - cd frontend
    - flutter pub get
  script:
    - flutter test --file-reporter "json:result.json"
  artifacts:
    paths: 
      - frontend/result.json
  rules:
    - exists:
      - frontend/test/*
  needs:
    - job: build-frontend
      optional: true
    - job: build-backend
      optional: true
